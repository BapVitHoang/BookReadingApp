<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_MEDIA_PLAYBACK" />
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.BookReadingApp">

        <activity
            android:name=".view.activity.LoginActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity android:name=".view.activity.MainActivity" android:exported="false" />
        <activity android:name=".view.activity.RegisterActivity" android:exported="false" />
        <activity android:name=".view.activity.BookDetailActivity" android:exported="false" />
        <activity android:name=".view.activity.ReadingActivity" android:exported="false" />
        <activity android:name=".view.activity.AudioPlayerActivity" android:exported="false" />
        <activity android:name=".view.activity.PodCourseDetailActivity" android:exported="false" />

        <service
            android:name=".service.AudioService"
            android:exported="false"
            android:foregroundServiceType="mediaPlayback" />

        <service
            android:name=".service.ReadingSyncService"
            android:exported="false" />

        <receiver
            android:name=".receiver.AudioControlReceiver"
            android:exported="false">
            <intent-filter>
                <action android:name="com.hcmute.bookreadingapp.action.AUDIO_PLAY" />
                <action android:name="com.hcmute.bookreadingapp.action.AUDIO_PAUSE" />
                <action android:name="com.hcmute.bookreadingapp.action.AUDIO_REWIND" />
            </intent-filter>
        </receiver>

    </application>

</manifest>
